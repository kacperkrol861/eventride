import { createVuestic } from "vuestic-ui";

export default createVuestic({
  config: {
    colors: {
      primary: "#2563eb",
      secondary: "#64748b",
      success: "#22c55e",
      danger: "#ef4444",
      warning: "#f59e0b"
    }
  }
});